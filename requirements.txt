# This file is kept for reference only.
# All dependencies are now managed via pyproject.toml and uv.
# To install: uv sync
# To add new dependencies: uv add <package>
# We shell out to ffmpeg via subprocess; ensure ffmpeg is installed on your system.